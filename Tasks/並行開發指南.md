# 並行開發快速參考指南

> **版本**: v1.0
> **建立日期**: 2025-12-17
> **目的**: 提供 AI Agent 並行開發時的快速參考

---

## 🎯 一分鐘速覽

```
總時間節省: 56% (從 11.5 天 → 5 天)

Phase 1-3:  串行執行（基礎設施）
Phase 4-16: 4 Agent 並行（13 頁面）← 最大效益
Phase 17-19: 3 Agent 並行（優化）
Phase 20:   串行執行（最終審查）
```

---

## 🔄 Phase 並行配置

### Foundation Layer (串行)

```
Phase 1 (基礎設施) → Phase 2 (設計系統) → Phase 3 (共用模組)
```

### Pages Layer (4 Agent 並行)

| Agent | 負責頁面 |
|-------|----------|
| **Agent-1** | Landing (P4) → Projects (P7) → Calendar (P10) → Profile (P12) |
| **Agent-2** | Dashboard (P5) → Data Table (P8) → Notifications (P11) |
| **Agent-3** | JS Showcase (P6) → Kanban (P9) → Settings General (P13) |
| **Agent-4** | Settings Billing (P14) → Security (P15) → README Viewer (P16) |

### Optimization Layer (3 Agent 並行)

| Agent | Phase | 前置條件 |
|-------|-------|----------|
| **Agent-A** | Phase 17 響應式 | 先調用 `rwd-design` Skill |
| **Agent-B** | Phase 18 無障礙 | 無 |
| **Agent-C** | Phase 19 效能 | 需調用 `codex-cli` Skill |

---

## 🚀 Sub-Agent 啟動指令

### Gemini CLI（文案/翻譯）

```bash
gemini -m gemini-2.5-pro -y "YOUR_PROMPT"
```

**常用 Prompt 模板**:

```bash
# 翻譯內容
gemini -m gemini-2.5-pro -y "為 NexusAI 生成中英雙語翻譯，輸出 JS 物件格式：[具體內容]"

# Landing Page 文案
gemini -m gemini-2.5-pro -y "為 NexusAI AI平台生成 Landing Page 文案（中/英）：Hero、特色、定價"

# 模擬數據
gemini -m gemini-2.5-pro -y "生成模擬數據 JSON：[具體需求]"
```

### code-reviewer Sub-agent（代碼審查）

```
[指令] 使用 code-reviewer Sub-agent 審查 Phase N 的所有代碼：
- 代碼品質
- 共用模組使用
- ARIA 無障礙
- 響應式斷點
```

### codex-cli Skill（演算法審查）

```
[指令] 使用 codex-cli Skill 審查以下演算法：
- [具體文件/函數]
```

### debugger Sub-agent（問題排查）

```
[指令] 使用 debugger Sub-agent 排查問題：
- 問題描述
- 重現步驟
```

### researcher Sub-agent（研究）

```
[指令] 使用 researcher Sub-agent 調查：
- [研究主題]
```

---

## ⚡ Phase 內部並行

### Phase 2 (設計系統)

可同時創建 6 個 CSS 文件：
- `design-tokens.css`
- `glassmorphism.css`
- `typography.css`
- `layout.css`
- `components.css`
- `animations.css`

### Phase 3 (共用模組)

```
Step 1: utils.js（必須先完成）
    ↓
Step 2: theme.js + i18n.js（可並行）
    ↓
Step 3: navbar.js + sidebar.js + footer.js（可並行）
    ↓
Step 4: toast.js + modal.js + page-template.js（可並行）
```

**AI 並行**: Claude 做邏輯 (Step 3.3) + Gemini 做翻譯 (Step 3.4) 同時進行

### 每個頁面內部

```
Claude Code ─────────────────┐
(HTML 結構 → CSS → JS 邏輯)   ├── 同時進行
                             │
Gemini CLI ──────────────────┘
(文案 → 翻譯 → 模擬數據)
```

---

## ✅ 強制檢查點

### 每個 Phase 完成後必須執行

```
□ 使用 code-reviewer Sub-agent 審查代碼
□ 修復審查發現的問題
□ 確認無問題後才進入下一 Phase
```

### 特殊 Phase 額外檢查

| Phase | 額外檢查 |
|-------|----------|
| Phase 3 | `[CODEX]` 審查工具函數效能 |
| Phase 5 | `[CODEX]` 審查 Chart.js 配置 |
| Phase 6 | `[CODEX]` 審查拖拽/排序演算法 |
| Phase 8 | `[CODEX]` 確認排序效能 |
| Phase 9 | `[CODEX]` 確認拖拽效能 |
| Phase 10 | `[CODEX]` 審查日期計算 |
| Phase 15 | `[CODEX]` 確認密碼強度演算法 |

---

## 📋 標籤速查

| 標籤 | 含義 | 調用方式 |
|------|------|----------|
| `[CLAUDE]` | Claude Code 主要開發 | 直接執行 |
| `[GEMINI]` | Gemini CLI 文案/翻譯 | `gemini -m gemini-2.5-pro -y` |
| `[CODEX]` | Codex CLI 演算法審查 | `codex-cli` Skill |
| `[REVIEW]` | code-reviewer 代碼審查 | Sub-agent |
| `[DEBUG]` | debugger 問題排查 | Sub-agent |
| `[DOC]` | doc-generator 文檔生成 | Sub-agent |
| `[RESEARCH]` | researcher 研究調查 | Sub-agent |
| `[RWD]` | rwd-design 響應式指南 | Skill |

---

## 🔀 並行標記說明

| 標記 | 含義 |
|------|------|
| 🔀 PARALLEL | 此區塊內的任務可並行執行 |
| ⏳ SEQUENTIAL | 此區塊必須串行執行 |
| 🔗 DEPENDENCY | 依賴前置任務完成 |

---

## 📊 時間估算

| 階段 | 串行 | 並行 | 節省 |
|------|------|------|------|
| Phase 1-3 | 3 天 | 2 天 | 33% |
| Phase 4-16 | 6.5 天 | 2 天 | 69% |
| Phase 17-19 | 1.5 天 | 0.5 天 | 67% |
| Phase 20 | 0.5 天 | 0.5 天 | 0% |
| **總計** | **11.5 天** | **5 天** | **56%** |

---

*建立日期: 2025-12-17*

